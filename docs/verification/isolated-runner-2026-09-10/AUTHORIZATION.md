# 本轮真实诊断授权与预注册

2026-09-10 用户要求后续业务验证使用真实服务、真实调用和真实结果，随后明确选择：“先做真实诊断，不计算人工质量分数”。本文件在任何本轮模型调用前保存。

这是对上一份输入协议中“先人审再测量”的限定修订：允许尚未人审的固定 development 输入做运行诊断，仍不允许将 AI 标签记作真人标签、输出准确率或发布级质量结论。旧协议、输入、holdout、run-1 不修改。

固定13题，单并发，每题一次；模型 gpt-5.6-luna / chat_completions，Embedding Qwen/Qwen3-Embedding-0.6B，复用1564×1024既有artifact，文档Embedding0。SDK20s/Python90s/Java105s/客户端130s，SDK/客户端0重试，Java最多2次；每类provider操作上限26、两类合计上限52，总时限35分钟。三次连续依赖失败、认证/配额、来源/trace漂移或未知程序错误停止，剩余题保留未执行，不为凑齐数量再次调用。

应用侧provider操作STARTED/终态记录不是供应商接收或账单证明。token仅在真实响应提供时报告，超时与中断的供应端消耗未知。网页原22/96报告仍是旧实验；本轮结果另存独立目录，未接入旧固定格式质量页。
