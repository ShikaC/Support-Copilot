# Support Copilot 协作规则

开始本项目的讲解、问答或优化前，必须先阅读：

- `MISSION.md`
- `docs/learning/TEACHING_PROTOCOL.md`
- `docs/learning/V1_PROJECT_MAP.md`
- `docs/learning/ONE_WEEK_INTERVIEW_SPRINT.md`
- `docs/learning/LIVE_RAG_COMPLETION_CRITERIA.md`
- `docs/optimizations/ROADMAP.md`

## 当前模式：理解优先、证据驱动

Support Copilot 已经积累了多轮跨服务优化。当前首要目标是让项目作者重新获得对代码、数据流和技术取舍的控制权，然后再继续演进为可运行、可使用、可验证的成熟 AI 应用。所有后续工作都必须同时服务于产品质量和项目作者的代码阅读、代码审核能力。

1. 新会话或上下文恢复后，先读取本文件、`MISSION.md`、`docs/learning/TEACHING_PROTOCOL.md`、`docs/learning/V1_PROJECT_MAP.md`、`docs/learning/ONE_WEEK_INTERVIEW_SPRINT.md`、`docs/learning/LIVE_RAG_COMPLETION_CRITERIA.md`、`docs/optimizations/ROADMAP.md` 和最近的交接提示词，再查看 `git status --short`、当前分支和最近提交。
2. 先区分代码已存在、历史证据已验证、当前工作区未提交改动和未来设计；历史提交或文档中的验证结果不能自动证明当前工作区仍然通过。
3. 在作者理解当前数据流、关键状态和失败语义前，不开始新的非阻塞功能开发。优先进行小范围代码导览、运行观察和完整讲解说明。
4. 每次只推进一个可验证的垂直切片。一个切片必须有业务问题、风险、影响文件、数据流、验收标准、测试或故障演练、文档记录和 Git 边界。
5. AI 先解释现有实现和修改理由，再提出最小改动；作者阅读 diff 并收到本切片讲解说明后，才进入下一切片。普通语法按需解释，不用连续功能生成替代理解。
6. 继续保留 mock、live 和 fallback 的明确边界。mock 只用于本地开发、测试和 CI；一次 live 成功不能推出模型质量、稳定性、生产规模或真实客服效果。
7. 推送、创建远端配置、使用真实外部 API、处理真实客户数据和其他外部写入必须得到用户明确授权；不得提交凭据或敏感原文。

每个切片的完整节奏是：

```text
说明业务问题和风险
-> 阅读当前代码并标出关键文件/行号
-> 只讲一个概念或一条紧密的数据流
-> 对该切片的关键数据流、失败语义和取舍做完整讲解说明
-> 设计最小修改
-> 实现、测试或故障演练
-> 作者检查 diff 和失败路径
-> 更新事实/决策/证据记录
-> 创建本地提交或明确保留未提交边界
```

完成标准不是“代码可以运行”，而是问题、风险、修改层、数据流、验证证据和当前限制都已讲解清楚并留有记录。

## OpenRAG 进入条件

仓库当前没有明确的 `OpenRAG` 路线、具体版本或接口约定。下一会话不得根据名称猜测其能力，也不得在没有基线的情况下把它加入主线。

1. 先确认 OpenRAG 的具体项目、版本、部署方式、许可证和要解决的问题；若信息不完整，记录为待确认设计输入。
2. 在现有 mock/live 检索和固定评估集稳定前，不引入 OpenRAG、第二套 RAG 流程、向量数据库或多模型路由。
3. 若确认融合，优先放在 Python 的知识检索/索引 seam。`AnalysisWorkflow`、Java 业务 API、工单状态、人工审核、权限和审计保持稳定。
4. OpenRAG 作为检索或索引 Adapter，必须返回现有契约需要的 chunk、来源、排名、分数、release/artifact、traceId 和失败语义。
5. 使用同一知识 corpus、同一评估集、同一安全规则比较当前实现和 OpenRAG。只有在至少一个预先定义的质量、延迟、成本或运维指标改善，且无证据安全率、引用正确性和权限过滤不下降时，才进入长期实现。
6. 如果当前实现与 OpenRAG 都需要存在，才建立真实的替换 seam；否则保持最小实现，避免为假设的扩展点增加抽象。

## 路线控制

当前主线按以下顺序推进：

```text
夺回项目控制权
-> 稳定工单分析与人工审核闭环
-> 固定 RAG 质量基线并修复 live 评估问题
-> 用同一基线评估 OpenRAG
-> 选择一个可信 Pilot 部署路径
-> 整理发布、演示、学习和简历证据
```

在“夺回项目控制权”完成前，暂停新增 Dashboard、多个供应商、多智能体、Redis、消息队列、Kubernetes 和其他没有明确容量或可靠性需求的基础设施。已有优化文档属于历史与证据材料，不把大量历史任务同时当作活跃待办。

项目状态应逐步收敛到少量权威文件：`docs/STATUS.md` 记录当前事实，`docs/ROADMAP.md` 只保留未来三个里程碑，`docs/decisions/ADR-*.md` 记录重要取舍，`docs/learning/READING_LOG.md` 记录作者已经理解的代码和流程。修改前先确认这些文件是否已经存在，避免创建重复的事实来源。

## 文档与交接写作规则

项目文档、优化记录、学习记录和交接提示词必须让下一位协作者能够在没有当前会话上下文的情况下判断“现在是什么、下一步是什么、做到什么算完成”。

1. 先写结论和当前事实，再写原因、实现细节和未来设想。
2. 明确标记 `已实现`、`已验证`、`当前工作区改动`、`blocked/partial` 和 `计划中`；代码存在不等于行为已验证，历史 SHA 证据不等于当前工作区证据。
3. 每条验证记录写明命令、Git SHA、工作树状态、结果、证据范围和不能证明的内容；没有人工 review 时，不写可发布的质量结论。
4. 每个路线项目写清目标、依赖、影响范围、验收标准、验证命令和剩余限制；避免只写“继续优化”“完善系统”等无法判断完成与否的句子。
5. 每份交接必须包含项目路径、用户目标、当前状态、关键事实、明确的第一步、后续顺序、停止条件和阻塞处理方式。
6. 事实只保留一个权威来源。其他文档使用清晰链接指向它，不复制会随代码变化的命令、字段表或状态数字。
7. OpenRAG 或其他未来技术在具体项目、版本、职责、引入理由和比较基线确定前，只能写成待确认输入，不能写成已选方案或已完成能力。

## 每轮交付的证据要求

每轮完成后必须明确记录：

- 修改或阅读的文件及关键位置。
- 原问题、风险和选择该修改层的原因。
- 正常路径和至少一个相关失败路径。
- 测试、故障演练、手动验证或无法验证的原因。
- 当前 Git SHA、工作区是否干净，以及证据是否绑定当前 SHA。
- 哪些事实可以写入简历，哪些仍然只能写成计划或限制。

未知程序错误保持为真实错误；只有可命名的外部依赖故障、证据不足或明确的业务降级才进入 fallback。不得通过吞错、删除测试、弱化门禁或修改文档措辞制造完成感。

## 深度讲解顺序

当用户主动要求深入代码、某个概念阻碍理解，或该概念属于面试关键技术时，必须按以下顺序进行：

1. 先用简单语言说明概念和它在项目里的作用。
2. 展示对应文件、准确行号和必要的代码片段。
3. 解释代码如何执行以及为什么这样写。
4. 给出一个具体输入、输出或运行示例。
5. 补充该机制的风险、边界、失败路径和当初未选择的替代方案。
6. 用几条要点收束本轮结论与限制，确保说明完整；不提问，也不要求作者作答。

仅仅提到一个名词、字段或文件，不代表已经讲过。不得要求用户根据未讲解的代码、字段或规则猜答案。

深度讲解每轮只能包含一个概念或一个紧密相关的代码点。普通语法和已经理解的内容不得在当前理解优先模式中反复重讲。

发生上下文压缩或更换协作者后，先重新读取上述文档和最近的优化记录，再继续讲解；不得从头重复已经讲过的内容。

所有功能取舍都应服务于 `MISSION.md`：把当前 V1 演进为可运行、可使用、可验证，并能为 AI 应用开发岗位提供真实简历证据的成熟项目。企业级表示工程能力和质量目标，不得虚构真实企业生产经历。
