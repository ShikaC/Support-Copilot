# Support Copilot 协作规则

开始本项目的讲解、问答或优化前，必须先阅读：

- `MISSION.md`
- `docs/learning/TEACHING_PROTOCOL.md`
- `docs/learning/V1_PROJECT_MAP.md`
- `docs/learning/ONE_WEEK_INTERVIEW_SPRINT.md`
- `docs/learning/LIVE_RAG_COMPLETION_CRITERIA.md`
- `docs/optimizations/ROADMAP.md`

## 当前七天冲刺模式

当前目标是在七天、每天约 5 小时的预算内形成可运行、可演示、可验证并能用于面试讲解的 V1.5 求职版本。冲刺期间，本节优先于后面的深度教学顺序：

1. 按一个可验证的功能批次推进，不按普通语法逐题推进。
2. 批次开始前只说明业务问题、关键数据流、改进方案、验证方式和能力边界。
3. 直接完成实现、测试、优化文档和本地 Git 提交，不在每个语法点停下来提问。
4. 每个批次只展示 2 至 3 个面试关键代码位置；普通语法按需解释。
5. 批次结束后，让用户用“问题、风险、修改层、流程、证据、限制”六项模板复述。
6. 每次最多提出一个与整个批次直接相关的复述问题，用户回答后再纠正。
7. 推送、创建远端配置等外部写入仍需用户明确授权。

不得因为加速而省略测试、故障演练、文档、提交或真实能力边界。

mock 模式只用于开发、测试和 CI。项目最终完成条件包含可接入正式模型与 Embedding API 的真实 RAG 链路，以及至少一次经过脱敏记录的真实 live 成功验证；不能在 mock-only 状态结束项目。

## 深度教学顺序

当用户主动要求深入代码、某个概念阻碍理解，或该概念属于面试关键技术时，必须按以下顺序进行：

1. 先用简单语言说明概念和它在项目里的作用。
2. 展示对应文件、准确行号和必要的代码片段。
3. 解释代码如何执行以及为什么这样写。
4. 给出一个具体输入、输出或运行示例。
5. 确认用户对已讲内容没有疑问。
6. 最后只提出一个完全基于已讲内容的问题，并等待用户回答。

仅仅提到一个名词、字段或文件，不代表已经教过。不得要求用户根据未讲解的代码、字段或规则猜答案。

深度讲解每轮只能包含一个概念或一个紧密相关的代码点。普通语法和已经理解的内容不得在冲刺期间反复出题。

发生上下文压缩或更换协作者后，先重新读取上述文档和最近的优化记录，再继续教学；不得从头重复已经完成的题目。

所有功能取舍都应服务于 `MISSION.md`：把当前 V1 演进为可运行、可使用、可验证，并能为 AI 应用开发岗位提供真实简历证据的成熟项目。企业级表示工程能力和质量目标，不得虚构真实企业生产经历。
